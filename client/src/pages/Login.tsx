/* Quiet Operations reminder: keep the form narrow, the reason visible, and all auth actions explicit as simulated prototype transitions. */
import { FormEvent, useState } from "react";
import { ArrowRight, Eye, EyeOff, LockKeyhole, Mail, ShieldCheck } from "lucide-react";
import { Link, useLocation } from "wouter";
import { toast } from "sonner";
import AuthLayout from "@/components/AuthLayout";

export default function Login() {
  const [, setLocation] = useLocation();
  const [showPassword, setShowPassword] = useState(false);
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const submit = (event: FormEvent) => { event.preventDefault(); toast.success("Demo sign-in accepted. Opening your station…"); setTimeout(() => setLocation("/app"), 500); };
  return <AuthLayout mode="login"><div><div className="qo-eyebrow text-[#e60012]">Operator access</div><h1 className="qo-display mt-4 text-[39px] font-semibold leading-[1.02] tracking-[-0.05em]">Good to see you<br />again.</h1><p className="mt-5 max-w-[360px] text-[14px] leading-[1.65] text-[#6d7785]">Pick up where your team left off. Your queue, guardrails, and open handoffs are waiting.</p><form onSubmit={submit} className="mt-9 space-y-5"><label className="block"><span className="mb-2 block text-[11px] font-bold text-[#536174]">Work email</span><span className="relative block"><Mail size={16} className="absolute left-3.5 top-1/2 -translate-y-1/2 text-[#97a3b1]" /><input required type="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="you@onfon.co.ke" className="h-12 w-full rounded-lg border border-[#dce3eb] bg-white pl-10 pr-4 text-[13px] outline-none transition-shadow placeholder:text-[#a6afba] focus:border-[#e60012] focus:ring-4 focus:ring-[#fff0f1]" /></span></label><label className="block"><div className="mb-2 flex items-center justify-between"><span className="text-[11px] font-bold text-[#536174]">Password</span><button type="button" onClick={() => toast.info("Password recovery is simulated in this UI-only prototype.")} className="text-[11px] font-bold text-[#e60012] hover:underline">Forgot password?</button></div><span className="relative block"><LockKeyhole size={16} className="absolute left-3.5 top-1/2 -translate-y-1/2 text-[#97a3b1]" /><input required type={showPassword ? "text" : "password"} value={password} onChange={(e) => setPassword(e.target.value)} placeholder="Enter your password" className="h-12 w-full rounded-lg border border-[#dce3eb] bg-white pl-10 pr-11 text-[13px] outline-none transition-shadow placeholder:text-[#a6afba] focus:border-[#e60012] focus:ring-4 focus:ring-[#fff0f1]" /><button type="button" aria-label={showPassword ? "Hide password" : "Show password"} onClick={() => setShowPassword(!showPassword)} className="absolute right-3.5 top-1/2 -translate-y-1/2 text-[#97a3b1] hover:text-[#e60012]">{showPassword ? <EyeOff size={16} /> : <Eye size={16} />}</button></span></label><button type="submit" className="qo-button flex h-12 w-full items-center justify-center rounded-lg bg-[#e60012] text-[12px] font-bold text-white shadow-[0_12px_24px_rgba(230,0,18,0.18)] hover:bg-[#b8000e]">Sign in to the station <ArrowRight size={15} className="ml-2" /></button></form><div className="my-7 flex items-center gap-3"><div className="h-px flex-1 bg-[#e5e9ef]" /><span className="text-[10px] font-bold uppercase tracking-[0.12em] text-[#a0a9b5]">or</span><div className="h-px flex-1 bg-[#e5e9ef]" /></div><button type="button" onClick={() => { toast.success("Demo SSO sign-in accepted."); setLocation("/app"); }} className="flex h-11 w-full items-center justify-center gap-2 rounded-lg border border-[#dce3eb] bg-white text-[12px] font-bold text-[#536174] transition-colors hover:border-[#e9b0b5] hover:text-[#e60012]">Continue with Onfon SSO <ShieldCheck size={15} /></button><p className="mt-7 text-center text-[12px] text-[#7b8795]">New to the station? <Link href="/signup" className="font-bold text-[#e60012] hover:underline">Create an operator account</Link></p></div></AuthLayout>;
}

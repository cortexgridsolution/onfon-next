/* Quiet Operations reminder: Settings is a control surface, not a preference dump—each toggle explains its consequence, permission, and persistence boundary. */
import { useState } from "react";
import { Bell, Check, ChevronDown, CircleHelp, Database, KeyRound, LockKeyhole, Mail, Palette, PlugZap, RotateCcw, Save, Settings2, ShieldCheck, Trash2, UserRound, Webhook, X } from "lucide-react";
import AppShell from "@/components/AppShell";
import StationInstrument from "@/components/StationInstrument";
import { useLocalStorage } from "@/hooks/useLocalStorage";
import { toast } from "sonner";

type Tab = "general" | "notifications" | "access" | "integrations" | "danger";
const tabs: { id: Tab; label: string; icon: typeof Settings2 }[] = [{ id: "general", label: "General", icon: Settings2 }, { id: "notifications", label: "Notifications", icon: Bell }, { id: "access", label: "Access", icon: LockKeyhole }, { id: "integrations", label: "Integrations", icon: PlugZap }, { id: "danger", label: "Danger zone", icon: Trash2 }];

export default function Settings() {
  const [tab, setTab] = useState<Tab>("general");
  const [workspace, setWorkspace] = useLocalStorage("onfon-next-workspace-name", "Onfon Next Kenya");
  const [timezone, setTimezone] = useLocalStorage("onfon-next-timezone", "Africa/Nairobi");
  const [compactRows, setCompactRows] = useLocalStorage("onfon-next-compact-rows", true);
  const [role, setRole] = useLocalStorage<"Operator" | "Admin">("onfon-next-role", "Operator");
  const [emailAlerts, setEmailAlerts] = useLocalStorage("onfon-next-email-alerts", true);
  const [stalledAlerts, setStalledAlerts] = useLocalStorage("onfon-next-stalled-alerts", true);
  const [weeklyDigest, setWeeklyDigest] = useLocalStorage("onfon-next-weekly-digest", false);
  const [confirmReset, setConfirmReset] = useState(false);
  const [saved, setSaved] = useState(false);
  const save = () => { setSaved(true); toast.success("Settings saved to this browser."); window.setTimeout(() => setSaved(false), 1800); };
  const toggle = (value: boolean, setValue: (value: boolean) => void) => setValue(!value);
  return <AppShell eyebrow="Workspace / configuration" title="Settings" action={<button type="button" onClick={save} className="qo-button flex items-center gap-2 rounded-lg bg-[#e60012] px-3.5 py-2.5 text-[11px] font-bold text-white shadow-[0_8px_18px_rgba(230,0,18,0.16)] hover:bg-[#b8000e]"><Save size={14} /> {saved ? "Saved" : "Save changes"}</button>}>
    <div className="mx-auto max-w-[1080px]"><StationInstrument items={[{ label: "Workspace", value: workspace, detail: "local operator profile", tone: "blue", icon: <Database size={14} /> }, { label: "Permission", value: role, detail: role === "Admin" ? "configuration visible" : "guardrails enforced", tone: role === "Admin" ? "amber" : "mint", icon: role === "Admin" ? <KeyRound size={14} /> : <ShieldCheck size={14} /> }, { label: "Persistence", value: "Browser local", detail: "no live credentials stored", tone: "mint", icon: <LockKeyhole size={14} /> }]} /><div className="mb-7"><p className="text-[13px] text-[#748091]">Keep the station tuned to the way your team operates.</p><p className="mt-1 text-[11px] text-[#9aa5b2]">These controls are local prototype preferences. Production secrets and permissions belong in your deployment environment.</p></div><div className="grid gap-6 lg:grid-cols-[220px_1fr]"><nav className="space-y-1 rounded-xl border border-[#dce3eb] bg-white p-2">{tabs.map(({ id, label, icon: Icon }) => <button key={id} type="button" onClick={() => setTab(id)} className={`flex w-full items-center gap-3 rounded-lg px-3 py-3 text-left text-[11px] font-bold transition-colors ${tab === id ? id === "danger" ? "border-l-2 border-[#bd6b60] bg-[#fffaf9] pl-[10px] text-[#bd6b60]" : "border-l-2 border-[#e60012] bg-[#fff0f1] pl-[10px] text-[#e60012]" : "border-l-2 border-transparent text-[#657181] hover:bg-[#f5f7fa]"}`}><Icon size={15} />{label}{id === "danger" && <span className="ml-auto h-1.5 w-1.5 rounded-full bg-[#bd6b60]" />}</button>)}</nav><section className="border-y border-[#dce3eb] bg-white">{tab === "general" && <div><div className="border-b border-[#edf0f4] px-5 py-4"><div className="flex items-center gap-2 text-[12px] font-bold"><Settings2 size={15} className="text-[#e60012]" /> General</div><p className="mt-1 text-[10px] text-[#8d98a5]">Name the station and tune the density of the operator desk.</p></div><div className="space-y-5 p-5"><label className="block"><span className="qo-eyebrow">Workspace name</span><input value={workspace} onChange={(e) => setWorkspace(e.target.value)} className="mt-2 h-10 w-full rounded-lg border border-[#dce3eb] px-3 text-[12px] font-bold text-[#3e4b5b] outline-none focus:border-[#e60012]" /></label><label className="block"><span className="qo-eyebrow">Timezone</span><div className="relative mt-2"><select value={timezone} onChange={(e) => setTimezone(e.target.value)} className="h-10 w-full appearance-none rounded-lg border border-[#dce3eb] bg-white px-3 pr-8 text-[12px] font-semibold text-[#607084] outline-none"><option>Africa/Nairobi</option><option>UTC</option><option>Europe/London</option></select><ChevronDown size={13} className="pointer-events-none absolute right-3 top-1/2 -translate-y-1/2 text-[#9aa5b2]" /></div></label><div className="flex items-center justify-between rounded-xl border border-[#e2e7ee] bg-[#fafbfd] p-4"><div><div className="text-[11px] font-bold">Compact rows</div><p className="mt-1 text-[10px] leading-[1.5] text-[#8d98a5]">Use denser list rows across Customers, Merchants, and Orders.</p></div><button type="button" onClick={() => toggle(compactRows, setCompactRows)} aria-pressed={compactRows} className={`relative h-6 w-11 rounded-full transition-colors ${compactRows ? "bg-[#e60012]" : "bg-[#cfd6df]"}`}><span className={`absolute top-1 h-4 w-4 rounded-full bg-white transition-transform ${compactRows ? "left-6" : "left-1"}`} /></button></div><div className="flex items-start gap-2 border-t border-[#edf0f4] pt-4 text-[10px] leading-[1.5] text-[#7f8d9d]"><Palette size={13} className="mt-0.5 shrink-0 text-[#e60012]" /> Quiet Operations stays mineral-white, red-and-ink-led, and coral-only for risk regardless of density.</div></div></div>}{tab === "notifications" && <div><div className="border-b border-[#edf0f4] px-5 py-4"><div className="flex items-center gap-2 text-[12px] font-bold"><Bell size={15} className="text-[#e60012]" /> Notifications</div><p className="mt-1 text-[10px] text-[#8d98a5]">Choose which operating signals should interrupt the desk.</p></div><div className="space-y-3 p-5">{[{ label: "Exception alerts", description: "Notify when an order is stuck longer than the policy window.", value: stalledAlerts, set: setStalledAlerts, icon: <ShieldCheck size={15} /> }, { label: "Email alerts", description: "Send high-severity suppression and import validation alerts to the operator inbox.", value: emailAlerts, set: setEmailAlerts, icon: <Mail size={15} /> }, { label: "Weekly performance digest", description: "Summarize attach rate, fulfilment, gross profit, and merchant health every Monday.", value: weeklyDigest, set: setWeeklyDigest, icon: <BarChart3Icon /> }].map((item) => <div key={item.label} className="flex items-center justify-between gap-4 rounded-xl border border-[#e2e7ee] bg-[#fafbfd] p-4"><div className="flex items-start gap-3"><span className="mt-0.5 text-[#e60012]">{item.icon}</span><div><div className="text-[11px] font-bold">{item.label}</div><p className="mt-1 max-w-[430px] text-[10px] leading-[1.5] text-[#8d98a5]">{item.description}</p></div></div><button type="button" onClick={() => toggle(item.value, item.set)} aria-pressed={item.value} className={`relative h-6 w-11 shrink-0 rounded-full transition-colors ${item.value ? "bg-[#e60012]" : "bg-[#cfd6df]"}`}><span className={`absolute top-1 h-4 w-4 rounded-full bg-white transition-transform ${item.value ? "left-6" : "left-1"}`} /></button></div>)}<div className="flex items-start gap-2 border-t border-[#edf0f4] pt-4 text-[10px] leading-[1.5] text-[#7f8d9d]"><CircleHelp size={13} className="mt-0.5 shrink-0 text-[#e60012]" /> Notification delivery is represented as a local preference until an email or push connector is configured.</div></div></div>}{tab === "access" && <div><div className="border-b border-[#edf0f4] px-5 py-4"><div className="flex items-center gap-2 text-[12px] font-bold"><LockKeyhole size={15} className="text-[#e60012]" /> Access & permissions</div><p className="mt-1 text-[10px] text-[#8d98a5]">Preview how the station hides sensitive controls from an Operator.</p></div><div className="space-y-5 p-5"><div className="grid gap-3 sm:grid-cols-2"><button type="button" onClick={() => setRole("Operator")} className={`rounded-xl border p-4 text-left ${role === "Operator" ? "border-[#e60012] bg-[#fff7f7]" : "border-[#e2e7ee] bg-white"}`}><div className="flex items-center justify-between"><span className="flex h-8 w-8 items-center justify-center rounded-lg bg-[#fff0f1] text-[#e60012]"><UserRound size={15} /></span>{role === "Operator" && <Check size={15} className="text-[#e60012]" />}</div><div className="mt-4 text-[12px] font-bold">Operator</div><p className="mt-1 text-[10px] leading-[1.5] text-[#8d98a5]">Can review customers, progress orders, and preview decisions. Rule editing and lever changes stay hidden.</p></button><button type="button" onClick={() => setRole("Admin")} className={`rounded-xl border p-4 text-left ${role === "Admin" ? "border-[#6d6d6d] bg-[#fffaf1]" : "border-[#e2e7ee] bg-white"}`}><div className="flex items-center justify-between"><span className="flex h-8 w-8 items-center justify-center rounded-lg bg-[#fff4e7] text-[#6d6d6d]"><KeyRound size={15} /></span>{role === "Admin" && <Check size={15} className="text-[#6d6d6d]" />}</div><div className="mt-4 text-[12px] font-bold">Admin</div><p className="mt-1 text-[10px] leading-[1.5] text-[#8d98a5]">Can tune pricing, edit automations, approve templates, and lift manual suppressions.</p></button></div><div className="rounded-xl border border-[#f0d8da] bg-[#fff7f7] p-4 text-[11px] leading-[1.6] text-[#667589]"><div className="flex items-center gap-2 font-bold text-[#e60012]"><ShieldCheck size={14} /> Current permission preview: {role}</div><p className="mt-2">Switching roles updates the Governance view and local control visibility immediately. This is a UI demonstration, not real authentication.</p></div></div></div>}{tab === "integrations" && <div><div className="border-b border-[#edf0f4] px-5 py-4"><div className="flex items-center gap-2 text-[12px] font-bold"><PlugZap size={15} className="text-[#e60012]" /> Integrations</div><p className="mt-1 text-[10px] text-[#8d98a5]">Connect the systems that bring orders, inventory, and messages into one station.</p></div><div className="space-y-3 p-5">{[{ label: "Orders & inventory source", description: "Import customers, merchants, catalogue, and order state from a system of record.", status: "Not connected", icon: <Database size={15} /> }, { label: "WhatsApp agent handoff", description: "Generate approved-template checkout links that open the customer conversation.", status: "Preview only", icon: <Webhook size={15} /> }, { label: "Email delivery", description: "Deliver exception alerts and weekly analytics digests to operators.", status: "Not connected", icon: <Mail size={15} /> }].map((item) => <div key={item.label} className="flex items-center justify-between gap-4 rounded-xl border border-[#e2e7ee] bg-[#fafbfd] p-4"><div className="flex items-start gap-3"><span className="mt-0.5 text-[#e60012]">{item.icon}</span><div><div className="text-[11px] font-bold">{item.label}</div><p className="mt-1 max-w-[430px] text-[10px] leading-[1.5] text-[#8d98a5]">{item.description}</p></div></div><div className="flex items-center gap-3"><span className={`rounded-full px-2 py-1 text-[9px] font-bold ${item.status === "Preview only" ? "bg-[#fff0f1] text-[#e60012]" : "bg-[#f0f3f7] text-[#7d8997]"}`}>{item.status}</span><button type="button" onClick={() => toast.info(`${item.label} connector setup is staged for this demo.`)} className="rounded-lg border border-[#dce3eb] px-3 py-2 text-[10px] font-bold text-[#607084] hover:border-[#e9b0b5] hover:text-[#e60012]">Configure</button></div></div>)}</div></div>}{tab === "danger" && <div><div className="border-b border-[#f0d8d4] bg-[#fffaf9] px-5 py-4"><div className="flex items-center gap-2 text-[12px] font-bold text-[#bd6b60]"><Trash2 size={15} /> Danger zone</div><p className="mt-1 text-[10px] text-[#9d7e7a]">Destructive actions affect only this browser’s prototype state.</p></div><div className="space-y-5 p-5"><div className="rounded-xl border border-[#f0d8d4] bg-[#fffaf9] p-4"><div className="text-[11px] font-bold text-[#536174]">Reset local demo state</div><p className="mt-1 text-[10px] leading-[1.5] text-[#8d98a5]">Clear saved pricing levers, statuses, approvals, generated links, role preview, and imported records. Seed data remains available.</p><button type="button" onClick={() => setConfirmReset(true)} className="mt-4 flex items-center gap-2 rounded-lg border border-[#f0d8d4] px-3 py-2.5 text-[10px] font-bold text-[#bd6b60] hover:bg-white"><RotateCcw size={13} /> Reset local state</button></div><div className="flex items-start gap-2 border-t border-[#edf0f4] pt-4 text-[10px] leading-[1.5] text-[#7f8d9d]"><CircleHelp size={13} className="mt-0.5 shrink-0 text-[#e60012]" /> Production data is not touched by this prototype control.</div></div></div>}</section></div></div>{confirmReset && <div className="fixed inset-0 z-[60] flex items-center justify-center bg-[#202b37]/35 p-4 backdrop-blur-sm"><div className="w-full max-w-[430px] rounded-2xl border border-white/80 bg-[#fbfcfe] p-6 shadow-[0_30px_100px_rgba(31,48,70,0.2)]"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-[#fff0ed] text-[#bd6b60]"><Trash2 size={18} /></div><h2 className="mt-5 text-[18px] font-bold tracking-[-0.035em]">Reset local station state?</h2><p className="mt-2 text-[12px] leading-[1.6] text-[#718092]">This is irreversible in the current browser, but it does not delete any production data because this prototype has no backend.</p><div className="mt-6 flex gap-3"><button type="button" onClick={() => setConfirmReset(false)} className="flex-1 rounded-lg border border-[#dce3eb] py-3 text-[11px] font-bold text-[#607084]">Keep state</button><button type="button" onClick={() => { Object.keys(localStorage).filter((key) => key.startsWith("onfon-next-" )).forEach((key) => localStorage.removeItem(key)); toast.success("Local prototype state cleared. Refresh any open route to reload seed data."); setConfirmReset(false); }} className="flex-1 rounded-lg bg-[#bd6b60] py-3 text-[11px] font-bold text-white">Confirm reset</button></div></div></div>}</AppShell>;
}

function BarChart3Icon() {
  return <span className="flex h-4 w-4 items-end gap-0.5" aria-hidden="true"><span className="h-2 w-1 rounded-sm bg-current" /><span className="h-3 w-1 rounded-sm bg-current" /><span className="h-4 w-1 rounded-sm bg-current" /></span>;
}
